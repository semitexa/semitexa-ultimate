<article class="guide">
    <span class="guide__index">{{ index }}</span>
    <span class="guide__eyebrow">{{ trans('hello.card_eyebrow') }}</span>
    <h2>{{ title }}</h2>
    <p>{{ bodyHtml|raw }}</p>
</article>
